{
  "name": "@operon/chronicle",
  "version": "0.0.0",
  "description": "The colony's audit mirror: a central D1 database every Gatekeeper reflects its ledger events and message bodies into, best-effort and never load-bearing. The Durable Objects stay the source of truth; this is the introspectable, queryable projection (and the operator console's data layer). Drizzle schema, committed SQL migrations.",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "sideEffects": false,
  "exports": {
    "./package.json": "./package.json",
    ".": {
      "@operon/source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "migrations",
    "!**/*.tsbuildinfo"
  ],
  "nx": {
    "includedScripts": []
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "drizzle-kit": "^0.31.10",
    "wrangler": "^4.118.0"
  }
}
